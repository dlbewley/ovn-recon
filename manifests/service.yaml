---
apiVersion: v1
kind: Service
metadata:
  name: ovn-recon
  labels:
    app: ovn-recon
  annotations:
    service.alpha.openshift.io/serving-cert-secret-name: plugin-serving-cert
spec:
  selector:
    app: ovn-recon
  ports:
    - name: https
      port: 9443
      targetPort: 9443
      protocol: TCP
  type: ClusterIP

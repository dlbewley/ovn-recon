# Beads Configuration File
# Team defaults for this repository. Local runtime state lives in
# .beads/metadata.json and the embedded Dolt store.

issue-prefix: "ovn-recon"

# Canonical issue data is Dolt (embedded store + bd dolt push / bd dolt pull).
# Do not rely on committing JSONL for sync; see docs/SYNC_CONCEPTS upstream.
export.auto: false

sync.remote: "git@github.com:dlbewley/ovn-recon.git"

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: ovnrecons.recon.bewley.net
spec:
  group: recon.bewley.net
  names:
    kind: OvnRecon
    listKind: OvnReconList
    plural: ovnrecons
    singular: ovnrecon
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OvnRecon is the Schema for the ovnrecons API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OvnReconSpec defines the desired state of OvnRecon.
            properties:
              collector:
                description: Collector configuration.
                properties:
                  enabled:
                    description: Enabled toggles logical topology features backed
                      by the collector service.
                    type: boolean
                  image:
                    description: Image configuration for the OVN collector container
                      image.
                    properties:
                      pullPolicy:
                        type: string
                      repository:
                        default: quay.io/dbewley/ovn-collector
                        type: string
                      tag:
                        type: string
                    type: object
                  logging:
                    description: Logging controls for the collector service.
                    properties:
                      includeProbeOutput:
                        default: false
                        type: boolean
                      level:
                        default: info
                        enum:
                        - error
                        - warn
                        - info
                        - debug
                        - trace
                        type: string
                    type: object
                  probeNamespaces:
                    default:
                    - openshift-ovn-kubernetes
                    - openshift-frr-k8s
                    description: ProbeNamespaces defines namespaces where collector
                      is allowed to probe OVN pods.
                    items:
                      type: string
                    type: array
                type: object
              collectorImage:
                description: |-
                  Deprecated: use collector.image instead.
                  CollectorImage configuration for the OVN collector container image.
                properties:
                  pullPolicy:
                    type: string
                  repository:
                    default: quay.io/dbewley/ovn-collector
                    type: string
                  tag:
                    type: string
                type: object
              collectorProbeNamespaces:
                default:
                - openshift-ovn-kubernetes
                - openshift-frr-k8s
                description: |-
                  Deprecated: use collector.probeNamespaces instead.
                  CollectorProbeNamespaces defines namespaces where collector is allowed to probe OVN pods.
                items:
                  type: string
                type: array
              consolePlugin:
                description: ConsolePlugin configuration
                properties:
                  displayName:
                    type: string
                  enabled:
                    type: boolean
                  image:
                    description: Image configuration for the plugin container.
                    properties:
                      pullPolicy:
                        type: string
                      repository:
                        default: quay.io/dbewley/ovn-recon
                        type: string
                      tag:
                        type: string
                    type: object
                  logging:
                    description: Logging controls for the console plugin backend.
                    properties:
                      accessLog:
                        properties:
                          enabled:
                            default: false
                            type: boolean
                        type: object
                      level:
                        default: info
                        enum:
                        - error
                        - warn
                        - info
                        - debug
                        type: string
                    type: object
                type: object
              featureGates:
                description: |-
                  Deprecated: use collector.enabled instead.
                  FeatureGates controls optional OVN Recon capabilities.
                properties:
                  ovn-collector:
                    default: false
                    description: OVNCollector enables logical topology features backed
                      by the collector service.
                    type: boolean
                type: object
              image:
                description: |-
                  Deprecated: use consolePlugin.image instead.
                  Image configuration for the plugin container.
                properties:
                  pullPolicy:
                    type: string
                  repository:
                    default: quay.io/dbewley/ovn-recon
                    type: string
                  tag:
                    type: string
                type: object
              operator:
                description: Operator configuration.
                properties:
                  logging:
                    description: Logging controls for the operator controller.
                    properties:
                      events:
                        description: Events controls Kubernetes Event behavior from
                          the operator.
                        properties:
                          dedupeWindow:
                            default: 5m
                            type: string
                          minType:
                            default: Normal
                            enum:
                            - Normal
                            - Warning
                            type: string
                        type: object
                      level:
                        default: info
                        enum:
                        - error
                        - warn
                        - info
                        - debug
                        - trace
                        type: string
                    type: object
                type: object
              targetNamespace:
                default: ovn-recon
                description: |-
                  TargetNamespace is where the OVN Recon workload and Service are created.
                  Defaults to "ovn-recon" when omitted.
                minLength: 1
                type: string
            type: object
          status:
            description: OvnReconStatus defines the observed state of OvnRecon.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: false
    storage: false
    subresources:
      status: {}
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: OvnRecon is the Schema for the ovnrecons API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OvnReconSpec defines the desired state of OvnRecon.
            properties:
              collector:
                description: Collector configuration.
                properties:
                  enabled:
                    description: Enabled toggles logical topology features backed
                      by the collector service.
                    type: boolean
                  image:
                    description: Image configuration for the OVN collector container
                      image.
                    properties:
                      pullPolicy:
                        type: string
                      repository:
                        default: quay.io/dbewley/ovn-collector
                        type: string
                      tag:
                        type: string
                    type: object
                  logging:
                    description: Logging controls for the collector service.
                    properties:
                      includeProbeOutput:
                        default: false
                        type: boolean
                      level:
                        default: info
                        enum:
                        - error
                        - warn
                        - info
                        - debug
                        - trace
                        type: string
                    type: object
                  probeNamespaces:
                    default:
                    - openshift-ovn-kubernetes
                    - openshift-frr-k8s
                    description: ProbeNamespaces defines namespaces where collector
                      is allowed to probe OVN pods.
                    items:
                      type: string
                    type: array
                type: object
              collectorImage:
                description: |-
                  Deprecated: use collector.image instead.
                  CollectorImage configuration for the OVN collector container image.
                properties:
                  pullPolicy:
                    type: string
                  repository:
                    default: quay.io/dbewley/ovn-collector
                    type: string
                  tag:
                    type: string
                type: object
              collectorProbeNamespaces:
                default:
                - openshift-ovn-kubernetes
                - openshift-frr-k8s
                description: |-
                  Deprecated: use collector.probeNamespaces instead.
                  CollectorProbeNamespaces defines namespaces where collector is allowed to probe OVN pods.
                items:
                  type: string
                type: array
              consolePlugin:
                description: ConsolePlugin configuration
                properties:
                  displayName:
                    type: string
                  enabled:
                    type: boolean
                  image:
                    description: Image configuration for the plugin container.
                    properties:
                      pullPolicy:
                        type: string
                      repository:
                        default: quay.io/dbewley/ovn-recon
                        type: string
                      tag:
                        type: string
                    type: object
                  logging:
                    description: Logging controls for the console plugin backend.
                    properties:
                      accessLog:
                        properties:
                          enabled:
                            default: false
                            type: boolean
                        type: object
                      level:
                        default: info
                        enum:
                        - error
                        - warn
                        - info
                        - debug
                        type: string
                    type: object
                type: object
              featureGates:
                description: |-
                  Deprecated: use collector.enabled instead.
                  FeatureGates controls optional OVN Recon capabilities.
                properties:
                  ovn-collector:
                    default: false
                    description: OVNCollector enables logical topology features backed
                      by the collector service.
                    type: boolean
                type: object
              image:
                description: |-
                  Deprecated: use consolePlugin.image instead.
                  Image configuration for the plugin container.
                properties:
                  pullPolicy:
                    type: string
                  repository:
                    default: quay.io/dbewley/ovn-recon
                    type: string
                  tag:
                    type: string
                type: object
              operator:
                description: Operator configuration.
                properties:
                  logging:
                    description: Logging controls for the operator controller.
                    properties:
                      events:
                        description: Events controls Kubernetes Event behavior from
                          the operator.
                        properties:
                          dedupeWindow:
                            default: 5m
                            type: string
                          minType:
                            default: Normal
                            enum:
                            - Normal
                            - Warning
                            type: string
                        type: object
                      level:
                        default: info
                        enum:
                        - error
                        - warn
                        - info
                        - debug
                        - trace
                        type: string
                    type: object
                type: object
              targetNamespace:
                default: ovn-recon
                description: |-
                  TargetNamespace is where the OVN Recon workload and Service are created.
                  Defaults to "ovn-recon" when omitted.
                minLength: 1
                type: string
            type: object
          status:
            description: OvnReconStatus defines the observed state of OvnRecon.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
